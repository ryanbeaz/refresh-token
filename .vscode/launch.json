{
    "version": "0.2.0",
    "configurations": [
      {
        "type": "pwa-chrome",
        "request": "launch",
        "name": "Launch Chrome Debugger",
        "url": "https://localhost:3000",
        "webRoot": "${workspaceFolder}"
      }
    ]
  }